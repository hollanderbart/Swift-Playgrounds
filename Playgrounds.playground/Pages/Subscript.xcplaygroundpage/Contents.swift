//: [Previous](@previous)

import Foundation

struct TimesTable {
    subscript(index: Int) -> Int {
        return 1
    }
}

let timeTable = TimesTable()
timeTable[1]  // return 1
timeTable[33] // also returns 1

//: [Next](@next)
